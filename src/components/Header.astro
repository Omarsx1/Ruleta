---
// Header component with Fortnite theme
---

<header class="backdrop-blur-[25px] bg-white/5 border-b border-white/20 shadow-[0_8px_32px_0_rgba(31,38,135,0.37)] sticky top-0 z-40">
  <div class="container mx-auto px-3 sm:px-6 lg:px-8 2xl:px-12 py-1 sm:py-1 lg:py-2 2xl:py-4">
    <div class="flex items-center justify-between">
      <!-- Logo and Title -->
      <div class="flex items-center space-x-2 sm:space-x-3 lg:space-x-4 2xl:space-x-6">
        <div class="w-5 h-5 sm:w-6 sm:h-6 lg:w-8 lg:h-8 2xl:w-12 2xl:h-12 bg-gradient-to-br from-orange-500 to-red-600 rounded-lg flex items-center justify-center shadow-lg">
          <img src="/F.svg" alt="F" class="w-2 h-2 sm:w-3 sm:h-3 lg:w-4 lg:h-4 2xl:w-6 2xl:h-6" style="filter: brightness(0) invert(1);" />
        </div>
        <div>
          <h1 class="text-white font-black text-xs sm:text-base lg:text-xl 2xl:text-3xl tracking-wide">
            RULETA <span class="text-fortnite-yellow">OMAR X SUCELY</span>
          </h1>
          <p class="text-white/70 text-xs sm:text-xs lg:text-xs 2xl:text-base font-medium hidden sm:block">
            Gira la ruleta y descubre tu destino
          </p>
        </div>
      </div>
      
      <!-- Store Button -->
      <a 
        href="/tienda" 
        class="backdrop-blur-[15px] bg-white/10 hover:bg-white/20 px-2 py-1 sm:px-2 sm:py-1 lg:px-3 lg:py-1 2xl:px-5 2xl:py-3 rounded-lg text-white font-semibold text-xs sm:text-xs lg:text-xs 2xl:text-lg border border-white/20 shadow-[0_4px_16px_0_rgba(31,38,135,0.3)] transform hover:scale-105 transition-all duration-300"
      >
        FORTNITE
      </a>
    </div>
  </div>
</header>

<style>
  header {
    position: relative;
    overflow: hidden;
  }
  
  header::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.2), transparent);
    animation: shimmer 3s infinite;
  }
  
  @keyframes shimmer {
    0% { left: -100%; }
    100% { left: 100%; }
  }
</style>
